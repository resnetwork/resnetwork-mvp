import { CalendarDays, MapPin } from "lucide-react";
import { EVENTS } from "../data/events";

export default function EventsSection() { return <div className="grid grid-cols-1 gap-6 md:grid-cols-2">{EVENTS.map((event) => <a key={event.slug} href={`/events/${event.slug}`} target="_blank" rel="noreferrer" className="group rounded-2xl bg-white p-7 transition-transform hover:-translate-y-1" style={{ boxShadow: "0 10px 30px rgba(6,29,61,.08)" }}><span className="text-xs font-extrabold tracking-wide" style={{ color: "#318455" }}>{event.category.toUpperCase()}</span><h3 className="mt-3 text-2xl font-bold" style={{ color: "#061D3D" }}>{event.title}</h3><p className="mt-3 text-sm leading-relaxed" style={{ color: "#52606F" }}>{event.summary}</p><div className="mt-5 space-y-2 text-sm font-semibold" style={{ color: "#245E42" }}><p className="flex gap-2"><CalendarDays size={16}/>{event.date}</p><p className="flex gap-2"><MapPin size={16}/>{event.location}</p></div><span className="mt-5 inline-block text-sm font-bold group-hover:underline" style={{ color: "#245E42" }}>Открыть страницу события ↗</span></a>)}</div>; }
